{
  "id": "page_views",
  "status": "implemented",
  "depends_on": [],
  "input": { "event_types": ["page_view"], "required_fields": ["site_id"] },
  "raw_event": { "preserve": true, "fields": ["event_id"] },
  "processor": { "facts": ["page_view_daily"], "rebuild": "incremental" },
  "api": { "query_surface": "overview", "routes": ["/v1/sites/{site_id}/overview"] },
  "dashboard": {
    "surface": "overview",
    "disabled": "baseline remains enabled",
    "empty": "zero metrics",
    "error": "generic error state"
  },
  "security": {
    "consent": "required",
    "origin_and_ingest_key": "enforced",
    "privacy": ["site isolation"]
  },
  "history": { "queryable_after_disabled": true, "backfill": "optional" }
}
