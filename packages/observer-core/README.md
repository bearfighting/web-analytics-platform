# @web-analytics/observer-core

Framework-neutral navigation contracts and the in-memory observer used by tests and development.

Browser and router integrations belong in adapter packages. The shared
navigation helpers are framework-agnostic and preserve the NavigationObserver
contract across router implementations.
