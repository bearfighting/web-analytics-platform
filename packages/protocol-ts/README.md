# @web-analytics/protocol-ts

TypeScript interfaces mirroring Event Protocol V1. JSON Schema remains the protocol source of truth.

This package contains no runtime browser or framework dependencies.
